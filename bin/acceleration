#!/usr/bin/env ruby

begin
  require 'acceleration'
rescue
  require 'rubygems'
  require 'acceleration'
end

require 'irb'
require 'irb/completion'
ENV['IRBRC'] = '.irbrc' if File.exist? '.irbrc'

ARGV.clear

IRB.start
exit!
